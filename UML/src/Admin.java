public class Admin extends User {
    Admin(String firstName,
                  String lastName,
                  String address,
                  String phoneNum,
                  int id) {
        super(firstName, lastName, address, phoneNum, id);
    }
}