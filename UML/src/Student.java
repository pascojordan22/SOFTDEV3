public class Student extends User {
    Student(String firstName,
            String lastName,
            String address,
            String phoneNum,
            int id) {
        super(firstName, lastName, address, phoneNum, id);
    }
}
